package me.Minecraftmage113.InsanityPlugin.helpers;

import org.bukkit.block.Block;
import org.bukkit.block.BlockFace;

public class Structure {
	int xOffset, yOffset, zOffset;
	Block origin;
	int[][][] structure; // {height{depth{width}}}
	public Structure(int xOffset, int yOffset, int zOffset, Block origin, int[][][] structure) {
		this.xOffset = xOffset;
		this.yOffset = yOffset;
		this.zOffset = zOffset;
		this.origin = origin;
		this.structure = structure;
	}
	public void setOrigin(Block origin) {
		this.origin = origin;
	}
	public boolean test() {
		//structure read as {height{depth{width}}}
		return false;
	}
	public void build(BlockFace facing) {
		int dzWidth = 0,
			dzDepth = 0,
			dxWidth = 0,
			dxDepth = 0;
		switch(facing) {
		case NORTH: //subtract depth from z, add width to x
			dzDepth = -1;
			dxWidth = 1;
		case EAST: //add depth to x, add width to z
		case SOUTH: //add depth to z, subtract width from x
		case WEST: //subtract depth from x,  subtract width from z
			
		default:
			break;
		
		}
	}
}
